## to install comment the last line, then run :
###        pip install -r tools.txt
## then uncomment detectron2 and install it by itself
streamlit-option-menu
mysql.connector
pyyaml==6.0
torch
torchvision -f https://download.pytorch.org/whl/torch_stable.html
opencv-python
#### Install after the lines above
# python -m pip install 'git+https://github.com/facebookresearch/detectron2.git'